---
title: This is a test post
author: Matt Dray
date: '2018-03-28'
categories:
  - R
tags:
  - plot
  - R Markdown
  - regression
slug: this-is-a-test-post
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
library(emo)
```

This is a test post. `r emo::ji("bug")`

```{r test-chunk-1, echo=TRUE, message=FALSE}
# test chunk 1
library(dplyr)
library(knitr)
head(iris)
```

```{r test-chunk-2, echo=TRUE, message=FALSE}
# test chunk 2
library(dplyr)
library(knitr)
iris %>%
  head() %>% 
  knitr::kable()
```

```{r test-chunk-3, echo=TRUE, message=FALSE}
# test chunk 3
library(dplyr)
library(ggplot2)
library(plotly)
p <- iris %>%
  ggplot2::ggplot() +
  geom_point(aes(x = Sepal.Length, y = Petal.Width))
ggplotly(p)
```

```{r map, echo=TRUE, message=FALSE}
# map
library(dplyr)
library(leaflet)
quakes %>% 
  slice(1:10) %>% 
  leaflet::leaflet() %>% 
  leaflet::addProviderTiles(providers$OpenStreetMap) %>% 
  leaflet::addMarkers(popup = ~paste("Magnitude", mag))
```

# an image

![](/post/2018-03-28-this-is-a-test-post_files/cotton-stainer_yu-luck_noun_719982_cc.png)